{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "apt-get install unzip curl && curl -fsSL https://bun.sh/install | bash && ~/.bun/bin/bun install --frozen-lockfile",
  "buildCommand": "~/.bun/bin/bun run build"
}
